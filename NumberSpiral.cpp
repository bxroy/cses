#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t; cin >> t;
  while (t--) {
    int y, x; cin >> y >> x;
    int id=y*x, n=1, cnt=0, yt=1, xt=1;

    bool ok=true;
    while(n!=id){
      while(y!=)
    }
    cout << cnt << endl;
  }
  return 0;
}
